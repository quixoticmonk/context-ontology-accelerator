# Document processing — [pdf,docx] extras pull in all transitive deps
# (unstructured-inference, spacy, pi-heif, etc.) needed at import time.
# We use strategy="fast" at runtime to skip ML model inference.
unstructured[pdf,docx]>=0.27.6

# Scanned PDF detection + page rendering for Textract.
# pillow is used directly to encode rendered pages as PNG (bitmap.to_pil()).
pypdfium2>=4.30.0
pillow>=12.3.0
