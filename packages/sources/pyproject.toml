[project]
name = "coa-sources"
version = "0.3.3"
description = "Unified Sources for Context Ontology Accelerator"
requires-python = ">=3.12"

dependencies = [
    "python-ulid>=3.0.0",
    "coa-common",
    "coa-control-plane-server",
    "boto3>=1.35.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/coa_sources"]

[dependency-groups]
integ = [
    "requests>=2.34.2",
    "pytest-xdist>=3.0",
    # Parses the induced ontology in the vocabulary-adherence test, which asserts
    # on owl:Class / skos:Concept rather than on raw Turtle text.
    "rdflib>=7.0",
]

[tool.pytest.ini_options]
markers = [
    "unit: Unit tests",
    "integ: Integration tests",
    "real_glue_ownership: Opt out of the unit conftest's Glue ownership stub and exercise the real check",
]
pythonpath = ["."]

[tool.uv.sources]
coa-common = { workspace = true }
coa-control-plane-server = { workspace = true }
